[project]
name = "ltexperiments"
description = "Context window sampling for LTXV / LTX-2 (LTXAV) video and audio models, including IC-LoRA guide splitting and per-window guide insertion across context windows. Self-contained and uses ComfyUI's context_handler and wrapper hooks without editing core."
version = "0.2.0"
license = {file = "LICENSE"}

[project.urls]
Repository = "https://github.com/drozbay/LTExperiments"
#  Used by Comfy Registry https://registry.comfy.org

[tool.comfy]
PublisherId = "drozbay"
DisplayName = "LTExperiments"
Icon = ""
